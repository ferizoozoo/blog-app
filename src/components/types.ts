export interface Post {
    text: string;
    title: string;
    date: string;
    image: string;
    author: string;
}

export interface Tag {
    name: string;
    count: number;
}

export interface Serie {
    name: string
}